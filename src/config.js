const config = {
    user: "John Doe",
    systemPrompt: "You are a candidate that is going to be asked questions by an interviewer.",
    GPT_API_KEY: process.env.REACT_APP_GPT_API_KEY,
    debugMode: false,
  };
  
  export default config;